import 'package:flutter_memory_game_custom_theme_dependency_module/flutter_memory_game_custom_theme_dependency_module.dart';

num getFontSize(num fontSize) => fontSize.sp;

num getImageSize(num imageSize) => imageSize.sp;

num getHeight(num height) => height.h;

num getWidth(num width) => width.w;

num getRadius(num radius) => radius.r;
